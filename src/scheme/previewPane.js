"use strict";

import { ebb, nobel, snowWhite } from "../colours";

export const backgroundColour = snowWhite;

export const scrollbarThumbBackgroundColour = nobel;

export const scrollbarTrackBackgroundColour = ebb;

export const scrollbarCornerBackgroundColour = ebb;

export default {
  backgroundColour
};
