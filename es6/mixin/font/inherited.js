"use strict";

const inheritedFontMixin = `

  font-size: inherit;
  line-height: inherit;
  font-family: inherit;
  text-rendering: inherit;
  font-feature-settings: inherit;

`;

export default inheritedFontMixin;
