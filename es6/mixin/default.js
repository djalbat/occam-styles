"use strict";

export default function defaultMixin() {
  return (`

    font-size: 2.4rem;
    font-weight: normal;
    font-family: 'DejaVu Sans Mono', Menlo, 'Lucida Console', Monaco, monospace;
  
  `);
} ;
