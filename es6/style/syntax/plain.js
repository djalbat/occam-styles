"use strict";

import { colour } from "../../scheme/syntax/plain";

const plainSyntaxStyle = `

  .plain.syntax, 
  .plain.syntax > * { 
    color: ${colour}; 
  }

`;

export default plainSyntaxStyle;
